<resources xmlns:tools="http://schemas.android.com/tools">

  <!-- Main app theme -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#ffffff</item>
    <item name="android:forceDarkAllowed">false</item>
  </style>

  <!-- ANDROID 12+ SPLASH -->
  <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@android:color/white</item>

    <!-- Transparent drawable prevents app icon fallback -->
    <item name="windowSplashScreenAnimatedIcon">@drawable/transparent_splash</item>

    <!-- DO NOT add windowSplashScreenIconBackground -->
    <!-- DO NOT add windowSplashScreenBrandingImage -->

    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>

</resources>
